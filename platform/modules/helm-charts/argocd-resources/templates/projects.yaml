{{- range .Values.projects }}
kind: AppProject
apiVersion: argoproj.io/v1alpha1
metadata:
  name: {{ .name }}
  namespace: argocd
spec:
  sourceRepos:
  -  '*'
  destinations:
  - namespace: {{ .namespace }}
    server: https://kubernetes.default.svc

---
{{- end }}